import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Docs/Frontend/Schema Registry" />

<div class="doc-locale-en">

# Schema Registry

## Single extension point

- Runtime registry for public schema nodes lives in `frontend/composables/schema/usePublicSchemaRegistry.ts`.
- Section-level and page-level schema registration must go through `frontend/components/schema/PublicSchemaNode/PublicSchemaNode.vue` or `usePublicSchemaNode`.
- Final aggregated JSON-LD is rendered only by `frontend/components/schema/PublicSchemaJsonLd/PublicSchemaJsonLd.vue` from `layouts/default.vue`.

## Source of reusable schema builders

- Reusable schema factories live in `frontend/composables/schema/public-schema-contract.ts`.
- Add new schema types there first, then consume them from pages or reusable sections.
- Do not build large schema objects inline inside page components unless the data shape is truly route-local and cannot be reused.

## Adding a new schema type

1. Add a typed builder/helper to `public-schema-contract.ts`.
2. Reuse existing shared inputs (`siteUrl`, breadcrumbs, entity metadata) instead of duplicating mapping logic.
3. Register the produced node via `PublicSchemaNode` or `usePublicSchemaNode`.
4. Verify the node appears inside the aggregated `application/ld+json` output on the target public SSR page.
5. Update SSR/SEO docs and tests if the new schema type becomes mandatory.

## Scope rules

- Public schema is allowed only for `/`, `/catalog/**`, and `/content/**`.
- `layouts/account.vue`, `layouts/organizations.vue`, and admin routes must not render public schema.
- Private pages do not get `schema.org` requirements.

</div>

<div class="doc-locale-ru">

# Schema Registry

## Единая точка расширения

- Runtime-реестр public schema-узлов находится в `frontend/composables/schema/usePublicSchemaRegistry.ts`.
- Регистрация schema на уровне секции и страницы должна идти через `frontend/components/schema/PublicSchemaNode/PublicSchemaNode.vue` или `usePublicSchemaNode`.
- Финальный агрегированный JSON-LD рендерится только через `frontend/components/schema/PublicSchemaJsonLd/PublicSchemaJsonLd.vue` из `layouts/default.vue`.

## Источник переиспользуемых builder-ов

- Переиспользуемые factory/helper для schema находятся в `frontend/composables/schema/public-schema-contract.ts`.
- Новый schema-тип сначала добавляется туда, и только потом используется на страницах или в переиспользуемых секциях.
- Не собираем большие schema-объекты инлайном внутри page-компонентов, если эта логика может быть переиспользована.

## Как добавлять новый schema-тип

1. Добавить typed builder/helper в `public-schema-contract.ts`.
2. Переиспользовать общие входы (`siteUrl`, `breadcrumbs`, `entity metadata`), а не дублировать mapping-логику.
3. Зарегистрировать готовый node через `PublicSchemaNode` или `usePublicSchemaNode`.
4. Проверить, что node попадает в агрегированный `application/ld+json` на нужной public SSR-странице.
5. Обновить SSR/SEO docs и тесты, если новый schema-тип становится обязательным.

## Границы применения

- Public schema допустима только для `/`, `/catalog/**` и `/content/**`.
- `layouts/account.vue`, `layouts/organizations.vue` и admin-маршруты не должны рендерить public schema.
- Для private-страниц `schema.org` не является требованием.

</div>
